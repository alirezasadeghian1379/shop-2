onMounted(() => {
  if ("OTPCredential" in window) {
    const inputHidden = document.querySelector<HTMLInputElement>("#otpInputHidden");
    if (!inputHidden) return;
    navigator.credentials
        .get({ otp: { transport: ["sms"] } } as any)
        .then((otp: any) => {
          const code = otp.code;
          if (code.length === state.otp.length) {
            code.split("").forEach((c:any, i:any) => (state.otp[i] = c));
          }
          otpFormRef.value?.submit();
        })
        .catch((err) => console.log("OTP Web API failed", err));
  }
});
const handlePaste = (e: ClipboardEvent) => {
  const paste = e.clipboardData?.getData("text") || "";
  if (paste.length === state.otp.length) {
    state.otp = paste.split("");
  }
};

<input id="otpInputHidden" type="text" maxlength="4" autocomplete="one-time-code" class="hidden" />

کاربر گرامی
کد تایید شما
code: #CODE#
@service.maherkaran.com ##CODE#


---------------------------------------------------------------------------------

-- ai api

- https://api.bluesminds.com/wallet
- https://console.groq.com/dashboard/usage?tab=cost
- https://openrouter.ai/workspaces/default?utm_source=signup-success

--------------------------------------------------------------------------

-- karbordei

- https://www.fontyab.com/download?id=15105
- https://app.logonomy.ir/logo/preview/1293905
- https://undraw.co/illustrations
- https://colorhunt.co/
- https://www.pwabuilder.com/
- https://progressier.com/pwa-icons-and-ios-splash-screen-generator
- https://lordicon.com/icons/wired/outline?i=426-brain
- https://previewed.app/editor/3D/
- https://api.brsapi.ir/Panel/login.html
- https://downloads.php.net/~windows/releases/archives/
- http://tokaido.proxy.rlwy.net:15389/login => 9router-server
- https://3x-ui370-production-6691.up.railway.app/panel/clients => 3x-ui panle

--------------------------------------------------------------------------



-- 9router اتصال به Pm2 یا سرور

- PORT=20127 NODE_ENV=development pm2 start   /mise/installs/node/24.19.0/lib/node_modules/9router/app/custom-server.js   --name 9router   --cwd /mise/installs/node/24.19.0/lib/node_modules/9router/app   --interpreter "$(command -v node)"   -- --port 20127


--------------------------------------------------------------------------

-- crawler-env

APP_NAME=crawl
APP_ENV=local
APP_KEY=base64:QVklbF5dJct10v/vdrq1cwNsdjgOhjKXJ3inx48EpCM=
APP_DEBUG=true
APP_URL=http://127.0.0.1:8000
APP_URL2=https://api.viseh.app/api/v1

APP_LOCALE=fa
APP_FALLBACK_LOCALE=fa
APP_FAKER_LOCALE=fa_IR

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=crawl_db
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="hello@example.com"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"


NESHAN_SEARCH_RADIUS_METERS=500
NESHAN_REQUEST_DELAY_SECONDS=10


--------------------------------------------------------------------------

-- email

- username = alirezasadeghian@atomicmail.io
- password = @Alireza135790#

--------------------------------------------------------------------------

-- github

- username = alirezasadeghian1379
- password = @Alireza135790#

--------------------------------------------------------------------------

-- auto sms ( android - ios )


onMounted(() => {
  if ("OTPCredential" in window) {
    const inputHidden = document.querySelector<HTMLInputElement>("#otpInputHidden");
    if (!inputHidden) return;
    navigator.credentials
        .get({ otp: { transport: ["sms"] } } as any)
        .then((otp: any) => {
          const code = otp.code;
          if (code.length === state.otp.length) {
            code.split("").forEach((c:any, i:any) => (state.otp[i] = c));
          }
          otpFormRef.value?.submit();
        })
        .catch((err) => console.log("OTP Web API failed", err));
  }
});
const handlePaste = (e: ClipboardEvent) => {
  const paste = e.clipboardData?.getData("text") || "";
  if (paste.length === state.otp.length) {
    state.otp = paste.split("");
  }
};

<input id="otpInputHidden" type="text" maxlength="4" autocomplete="one-time-code" class="hidden" />



-- template sms

کاربر گرامی
کد تایید شما
code: #CODE#
@service.maherkaran.com ##CODE#

--------------------------------------------------------------------------

-- افزودن ip جدید برای پروکسی



sudo ufw delete 13
sudo ufw delete 12
sudo ufw allow from NEW_PUBLIC_IP to any port 10809 proto tcp
sudo ufw deny 10809/tcp
sudo ufw reload

-- باز کردن کل پروکسی

sudo ufw delete deny 10809/tcp
sudo ufw allow 10809/tcp
sudo ufw reload


--------------------------------------------------------------------------

 -- firebase-plugin-front

import { defineNuxtPlugin } from '#app'
import { initializeApp, getApps } from 'firebase/app'
import { getMessaging, isSupported } from 'firebase/messaging'
import type { Messaging } from 'firebase/messaging'

declare module '#app' {
    interface NuxtApp {
        $messaging: Messaging | null
    }
}

declare module '@vue/runtime-core' {
    interface ComponentCustomProperties {
        $messaging: Messaging | null
    }
}

export default defineNuxtPlugin((nuxtApp) => {
    if (process.client) {
        const firebaseConfig = {
            apiKey: "AIzaSyCzd_o1MjprMXicBdQ1VA3ud_bJY8tHjWc",
            authDomain: "rahatgold-77507.firebaseapp.com",
            projectId: "rahatgold-77507",
            storageBucket: "rahatgold-77507.firebasestorage.app",
            messagingSenderId: "738365179577",
            appId: "1:738365179577:web:b80c62387468c8ad5a3d68"
        }

        if (!getApps().length) {
            initializeApp(firebaseConfig)
        }

        isSupported().then((supported) => {
            if (supported) {
                const messaging: Messaging = getMessaging()
                nuxtApp.provide('messaging', messaging)
            } else {
                console.warn('Firebase Messaging is not supported on this browser.')
                nuxtApp.provide('messaging', null)
            }
        })
    }
})


--------------------------------------------------------------------------

 -- disabled-log-plugins-front

 export default defineNuxtPlugin(() => {
     if (!import.meta.server) return
     console.log = () => {}
     console.info = () => {}
     console.warn = () => {}
 })

 --------------------------------------------------------------------------

 -- update-cache-plugins-front

 export default defineNuxtPlugin(() => {
     if (process.client) {
         const version = useNuxtApp().$config.app.buildId
         if (!version) return
         const lastVersion = localStorage.getItem('lastVersion')
         if (!lastVersion) {
             localStorage.setItem('lastVersion', version)
             return
         }
         if (lastVersion === version) return
         localStorage.clear()
         sessionStorage.clear()
         if ('serviceWorker' in navigator) {
             navigator.serviceWorker.getRegistrations().then((registrations) => {
                 registrations.forEach((registration) => registration.unregister())
             })
         }
         if ('caches' in window) {
             caches.keys().then((cacheNames) => {
                 cacheNames.forEach((cacheName) => caches.delete(cacheName))
             })
         }
         localStorage.setItem('lastVersion', version)
         window.location.reload()
     }
 })


--------------------------------------------------------------------------

 -- firebase-sw-front

 importScripts('https://www.gstatic.com/firebasejs/8.10.1/firebase-app.js');
 importScripts('https://www.gstatic.com/firebasejs/8.10.1/firebase-messaging.js');
 firebase.initializeApp({
   apiKey: "AIzaSyCzd_o1MjprMXicBdQ1VA3ud_bJY8tHjWc",
   authDomain: "rahatgold-77507.firebaseapp.com",
   projectId: "rahatgold-77507",
   storageBucket: "rahatgold-77507.firebasestorage.app",
   messagingSenderId: "738365179577",
   appId: "1:738365179577:web:b80c62387468c8ad5a3d68"
 });

 const messaging = firebase.messaging();


 self.addEventListener('push', function(event) {
   const payload = event.data?.json() || {};
   const notificationTitle = payload?.data.title || 'Title';
   const notificationOptions = {
     body: payload?.data.body || '',
     icon: payload?.data.icon || '/favicon.ico',
     data: {
       click_action: payload?.data.click_action || '/'
     }
   };
   event.waitUntil(
     self.registration.showNotification(notificationTitle, notificationOptions)
   );
 });

 self.addEventListener('notificationclick', function(event) {
   event.notification.close();
   const clickAction = event.notification.data?.click_action || '/';
   event.waitUntil(
     clients.matchAll({ type: 'window', includeUncontrolled: true }).then(windowClients => {
       for (let client of windowClients) {
         if (client.url === clickAction && 'focus' in client) return client.focus();
       }
       return clients.openWindow(clickAction);
     })
   );
 });


--------------------------------------------------------------------------


 - گذاشتن سرچ برای تمام بخش های ادمین پنل
 - گذاشتن بخش حذف گروهی برای تمام بخش های ادمین پنل
 - جدا کردن بخش کنفیگ های اس ام اس - درگاه پرداخت - ai و افزودن کانفیگ های آنها به بخش تنظیمات
 - متود getTemplateParameters باید قالب اس ام اس رو تغییر بدم به اسم قالب نه کد قالب
 - بخش لندینگ پیج
 - بخش تماس با ما
 - بخش دسته بندی ها
 - بخش مقالات
 -
